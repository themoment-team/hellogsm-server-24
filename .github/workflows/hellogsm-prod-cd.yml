name: HelloGSM prod CD workflow

on:
  push:
    branches:
      - 'main'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  CD:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout Code
        uses: actions/checkout@v7

      - name: Setup JDK 25
        uses: actions/setup-java@v5
        with:
          java-version: '25'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Make YML File
        run: touch ./server/src/main/resources/application-prod.yml

          echo "${{ secrets.PROD_WEB_YML }}" > ./server/src/main/resources/application-prod.yml

        shell: bash

      - name: Build with Gradle
        # 배포 산출물은 server 의 boot jar 하나뿐이고, 전체 그래프 검증은 같은 커밋에서 CI 가 맡는다.
        # server 는 persistence 만 의존하므로 entrance 모듈은 빌드 대상에서 빠진다.
        run: ./gradlew :server:build

      - name: Make ZIP File
        run: |
          mkdir -p deploy/build/libs
          cp server/build/libs/*.jar deploy/build/libs/
          cp appspec.yml deploy/
          cp DockerfileProd deploy/
          cp -r scripts deploy/
          cd deploy && zip -r ../$GITHUB_SHA.zip .
        shell: bash

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ vars.AWS_PROD_DEPLOY_ROLE_ARN }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Upload to S3
        run: aws s3 cp --region ${{ secrets.AWS_REGION }} ./$GITHUB_SHA.zip s3://${{ secrets.BUCKET_NAME }}/prod/$GITHUB_SHA.zip

      - name: Code Deploy
        run: aws deploy create-deployment --application-name hellogsm-prod-codedeploy --deployment-config-name CodeDeployDefault.AllAtOnce --deployment-group-name api-prod-hellogsm-kr --s3-location bucket=${{ secrets.BUCKET_NAME }},bundleType=zip,key=prod/$GITHUB_SHA.zip

  notify:
    runs-on: ubuntu-latest
    needs: [CD]
    if: always()

    steps:
      - name: Determine Status
        id: status
        run: |
          if [[ "${{ needs.CD.result }}" == "failure" ]]; then
            echo "color=0xFF4C4C" >> $GITHUB_OUTPUT
          elif [[ "${{ needs.CD.result }}" == "success" ]]; then
            echo "color=0x4CAF50" >> $GITHUB_OUTPUT
          else
            echo "color=0xFFA500" >> $GITHUB_OUTPUT
          fi

      - name: Send Discord Notification
        uses: sarisia/actions-status-discord@v1
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK }}
          status: ${{ needs.CD.result }}
          color: ${{ steps.status.outputs.color }}
